const helloWorld = () => "Hello World";

const helloName = (name) => `helloo my name is ${name}`;

const nameAge = (name, Age) => `hello my name is${name} and my Age is ${Age}`;

const callAge = (Age) => `hello my age is ${Age}`;

console.log(helloWorld());
